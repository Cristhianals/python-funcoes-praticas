# Função Máximo de Dois Números 🐍

## 📋 Enunciado

Escreva uma função que receba dois números como parâmetros e retorne o maior entre eles.

---

## 💡 Objetivo do exercício

- Praticar a criação de funções em Python.  
- Utilizar condicionais (`if`/`else`) para comparar valores.  
- Retornar valores de funções com `return`.  

---

## 🧠 Explicação do raciocínio

A função `maximo` recebe dois números (`a` e `b`) e compara-os:  
- Se `a` for maior que `b`, retorna `a`.  
- Caso contrário, retorna `b`.  
A função também imprime uma mensagem indicando que o valor retornado é o maior número.  
Isso ajuda a entender como condicional, impressão e retorno podem trabalhar juntos dentro de uma função.  

---

## 🚀 Código da solução

O código completo está disponível em [solucao.py](./solucao.py)

## 🖥 Exemplo de execução

```text
O maior número é:  
6  
O maior número é:  
2  
O maior número é:  
7  
```

---

## 🎯 Aprendizado

- Como criar funções simples em Python.
- Como usar if/else para lógica de comparação.
- Como retornar valores com return.
- Como imprimir informações ao usuário ao mesmo tempo em que se retorna um valor da função.
